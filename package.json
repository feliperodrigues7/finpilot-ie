{
  "name": "finpilot-ie",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && mkdir -p dist && cp -f admin.html dist/admin.html && cp -f admin.js dist/admin.js && cp -f privacy.html dist/privacy.html && cp -f favicon.ico dist/favicon.ico",
    "preview": "vite preview --port 4173",
    "lint": "echo \"no lint configured\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
